{
  "name": "hm-chatbot",
  "version": "1.0.0",
  "description": "WhatsApp Cloud API chatbot with Fastify and Prisma",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0",
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=20"
  }
}
